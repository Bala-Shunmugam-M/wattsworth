# WattsWorth dependencies — pinned to the versions the 61-test suite passes against.
# Tested on Python 3.13 (Windows). Update deliberately, then re-run: pytest tests/ -v
streamlit==1.58.0
pandas==3.0.3
numpy==2.4.6
statsmodels==0.14.6
scikit-learn==1.9.0
plotly==6.8.0
pytest==9.0.3
